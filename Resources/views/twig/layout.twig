<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Comibyte Framework{% endblock %}</title>

    <style>
        /* ------------------------------
           Global Reset
        ------------------------------ */
        *, *::before, *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Inter", Arial, sans-serif;
            background: #0f0f0f;
            color: #ffffff;
            line-height: 1.6;
            padding: 20px;
        }

        a {
            text-decoration: none;
        }

        /* ------------------------------
           Layout Container
        ------------------------------ */
        .container {
            max-width: 1200px;
            margin: auto;
        }

        /* ------------------------------
           Hero Section
        ------------------------------ */
        .hero {
            text-align: center;
            margin: 70px 0 50px;
            animation: fadeIn 1s ease-in-out;
        }

        .title {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 10px;
        }

        .title span {
            color: #4c6bafff;
        }

        .subtitle {
            font-size: 1.2rem;
            opacity: 0.8;
        }

        /* ------------------------------
           Button Styles
        ------------------------------ */
        .btn {
            display: inline-block;
            margin-top: 15px;
            padding: 12px 25px;
            background: #4c6bafff;
            color: #fff;
            border-radius: 6px;
            font-weight: 600;
            transition: 0.3s;
        }

        .btn:hover {
            background: #3b5386ff;
        }

        /* ------------------------------
           Grid Cards
        ------------------------------ */
        .grid {
            margin-top: 50px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 25px;
        }

        .card {
            background: #1c1c1c;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.4);
            transition: 0.3s;
        }

        .card:hover {
            transform: translateY(-5px);
            background: #222222;
        }

        .card h3 {
            margin-bottom: 10px;
            color: #4c6bafff;
        }

        .card p {
            opacity: 0.9;
        }

        /* ------------------------------
           Sections
        ------------------------------ */
        .section {
            margin: 60px 0;
        }

        .section h2 {
            font-size: 2rem;
            margin-bottom: 20px;
            border-left: 5px solid #4c6bafff;
            padding-left: 10px;
        }

        .features-list,
        .benefits-list {
            list-style: none;
            margin-left: 10px;
        }

        .feature-item,
        .benefit-item {
            margin: 10px 0;
            padding: 10px 15px;
            background: #1c1c1c;
            border-radius: 8px;
            border-left: 5px solid #4c6bafff;
        }

        /* ------------------------------
           CTA Section
        ------------------------------ */
        .cta {
            margin: 70px auto;
            text-align: center;
            background: #1a1a1a;
            padding: 30px;
            border-radius: 12px;
            border: 1px solid #333;
        }

        .cta p {
            font-size: 1.2rem;
            margin-bottom: 15px;
        }

        /* ------------------------------
           Footer
        ------------------------------ */
        .footer {
            margin-top: 70px;
            text-align: center;
            opacity: 0.6;
            font-size: 0.9rem;
            border-top: 1px solid #333;
            padding-top: 20px;
        }

        /* ------------------------------
           Animations
        ------------------------------ */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to   { opacity: 1; transform: translateY(0); }
        }

    </style>
</head>

<body>
    <div class="container">
        {% block content %}{% endblock %}
    </div>

    <div class="footer">
        <p>Powered by {{ framework }} | PHP {{ constant('PHP_VERSION') }}</p>
    </div>
</body>

</html>
